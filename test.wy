gimme x = 12 / (1 + 2 * 6 - 1); // 1
gimme y = x + 5;
gimme z = 1;

why (x < 10) {
    x = x + 1;
    yell("incrementing x\n");
}

loop (i in 1..11) {
    x = i;
    yell('hey\n');
}

maybe (z) {
    x = x + 1;
} but (1) { 
    x = x + 2;
} nah {
    x = x + 3;
}


/*
    l
    test2
    bye(20);
*/

gimme str = 'abcdefghijk';
str = 'abcfhgfdghdfügo0dfugdf\n';
yell(str);
bye(x);


