gimme x: number = 12 / (1 + 2 * 6 - 1); // 1
gimme y: number = x + 5;
gimme z: bool = yep;

why (x < 10) {
    x = x + 1;
    yell("incrementing x\n");
}

four (i in 1..11) {
    x = i;
    yell('hey\n');
}

maybe (z) {
    x = x + 1;
} but (1) { 
    x = x + 2;
} nah {
    x = x + 3;
}

thingy pow(num: number): number {
    gimmeback num * num;
}
gimme lol: number = pow(10);

/*
    l
    test2
    bye(20);
*/

gimme string: str = 'abcdefghijk';
string = 'abcfhgfdghdfügo0dfugdf\n';
yell(string + string);


